//Action types
const initialState = {
  pageSize: 10,
};
//Action creators

//Reducer
export default function appReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
